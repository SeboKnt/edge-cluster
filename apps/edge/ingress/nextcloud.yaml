---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  parentRefs:
  - name: shared-gateway
    namespace: kube-system
  hostnames:
  - "cloud.edge.youngandhungry.org"
  rules:
  - backendRefs:
    - name: nextcloud
      port: 8080
