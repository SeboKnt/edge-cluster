---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
  namespace: keycloak
spec:
  parentRefs:
  - name: shared-gateway
    namespace: kube-system
  hostnames:
  - "sso.edge.youngandhungry.org"
  rules:
  - backendRefs:
    - name: keycloak
      port: 80
