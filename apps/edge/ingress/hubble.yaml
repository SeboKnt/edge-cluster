---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble-ui
  namespace: kube-system
spec:
  parentRefs:
  - name: shared-gateway
    namespace: kube-system
  hostnames:
  - "hubble.edge.youngandhungry.org"
  rules:
  - backendRefs:
    - name: hubble-ui
      port: 80
