---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nexus
  namespace: nexus
spec:
  parentRefs:
  - name: shared-gateway
    namespace: kube-system
  hostnames:
  - "nexus.edge.youngandhungry.org"
  rules:
  - backendRefs:
    - name: nexus-nexus-repository-manager
      port: 8081
