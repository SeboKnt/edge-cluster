---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: openwebui
  namespace: ai
spec:
  parentRefs:
  - name: shared-gateway
    namespace: kube-system
  hostnames:
  - "openwebui.edge.youngandhungry.org"
  rules:
  - backendRefs:
    - name: openwebui-open-webui
      port: 80
